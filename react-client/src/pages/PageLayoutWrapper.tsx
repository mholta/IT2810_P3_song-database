import React from 'react';

interface PageLayoutWrapperProps {}

const PageLayoutWrapper = ({}: PageLayoutWrapperProps) => {
  return <div>PageLayoutWrapper</div>;
};

export default PageLayoutWrapper;
